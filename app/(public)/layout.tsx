/**
 * Public Route Group Layout
 * 
 * This layout wraps all public-facing routes including:
 * - /katalog
 * - Future public pages
 */

export default function PublicGroupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
